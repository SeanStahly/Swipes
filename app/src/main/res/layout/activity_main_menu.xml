<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    android:layout_marginLeft="@dimen/activity_horizontal_margin"
    android:layout_marginRight="@dimen/activity_horizontal_margin">

    <TextView
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:textAppearance="?android:attr/textAppearanceLarge"
        android:text="@string/app_name_caps"
        android:id="@+id/menu_title_text_view"
        android:layout_gravity="center_horizontal"
        android:layout_above="@+id/menu_play_button"
        android:layout_centerHorizontal="true"
        android:layout_marginBottom="54dp" />

    <Button
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="@string/menu_play"
        android:id="@+id/menu_play_button"
        android:layout_gravity="center_horizontal"
        android:layout_marginBottom="86dp"
        android:layout_above="@+id/menu_high_scores_button"
        android:layout_centerHorizontal="true"
        android:onClick="playGame"/>

    <Button
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="@string/menu_high_scores"
        android:id="@+id/menu_high_scores_button"
        android:layout_gravity="center_horizontal"
        android:layout_centerVertical="true"
        android:layout_centerHorizontal="true"
        android:onClick="viewHighScores"/>

    <Button
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="@string/menu_settings"
        android:id="@+id/menu_options_button"
        android:layout_gravity="center_horizontal"
        android:layout_below="@+id/menu_high_scores_button"
        android:layout_centerHorizontal="true"
        android:layout_marginTop="86dp"
        android:onClick="viewSettings"/>
</RelativeLayout>
